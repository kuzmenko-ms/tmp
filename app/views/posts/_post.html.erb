  <div id ="content"><tr>Пользователь <br></div>
<div id="kj"><%= post.dep %></div> 
<%= image_tag post.user.avatar.url("medium") %>
    <%= post.user.name %> <%= post.user.midname %> <%= post.user.surname %><br>
	<div id ="content">Тема<br></div>
    <%= post.title %><br>
<div id ="content">Отзыв об отделении<br></div>
    <%= post.content %><br>
    <% if can? :read, post %>
      <%= link_to 'Подробнее', post %>|
    <% end %>
    <% if can? :edit, post %>
      <%= link_to 'Редактировать', edit_post_path(post) %>|
    <% end %>
    
    <% if can? :destroy, post %>
      <%= link_to 'Удалить', post, confirm: 'Вы уверены?', method: :delete %>
    <% end %>
  _________________________________________________________________________________________________________

  </tr>

